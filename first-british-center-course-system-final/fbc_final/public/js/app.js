const state={user:null,csrf:'',page:'dashboard',cache:{},term:null};
const api=async(action,opts={})=>{let url='/api/?action='+encodeURIComponent(action);if(opts.q)url+='&'+new URLSearchParams(opts.q);let init={method:opts.method||'GET',headers:{'Accept':'application/json'}};if(opts.method&&opts.method!=='GET'){init.headers['Content-Type']='application/json';init.headers['X-CSRF-Token']=state.csrf;init.body=JSON.stringify(opts.body||{})}if(opts.form){init.method='POST';init.headers['X-CSRF-Token']=state.csrf;init.body=opts.form}const r=await fetch(url,init);const d=await r.json().catch(()=>({error:'Invalid server response'}));if(!r.ok)throw new Error(d.error||'Request failed');if(d.csrf)state.csrf=d.csrf;return d};
const $=(s,e=document)=>e.querySelector(s), $$=(s,e=document)=>[...e.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const toast=(m,bad=false)=>{let x=$('#toast');if(!x){x=document.createElement('div');x.id='toast';x.style='position:fixed;right:18px;bottom:18px;z-index:99;padding:12px 16px;border-radius:12px;color:#fff;box-shadow:0 12px 35px rgba(0,0,0,.2)';document.body.appendChild(x)}x.style.background=bad?'#b84343':'#263b73';x.textContent=m;x.style.display='block';setTimeout(()=>x.style.display='none',3200)};
async function boot(){const m=await api('me');state.user=m.user;state.csrf=m.csrf;if(!state.user){renderLogin();return}if(!state.user.profile_complete){renderOnboarding();return}renderShell();await loadPage('dashboard')}
function renderLogin(){document.body.innerHTML=`<div class="login"><div class="loginBrand"><img src="/assets/logo.png" alt="The First British Center"></div><div class="loginBox"><div class="panel"><div style="text-align:center"><img src="/assets/logo.png" style="width:170px;max-width:70%;margin-bottom:10px" alt="logo"><h1>Course Management</h1><p>Smart online course distribution & administration</p></div><form id="loginForm"><div class="field"><label>Username or Email</label><input name="login" required autocomplete="username"></div><div class="field"><label>Password</label><input name="password" type="password" required autocomplete="current-password"></div><button class="btn primary block">Sign in</button><div id="loginErr" class="error"></div></form><div class="divider">OR</div><a class="btn light block" href="/api/?action=google_start" style="display:block;text-align:center;text-decoration:none">Continue with Google</a><p style="font-size:12px;text-align:center;margin-top:18px">The First British Center for Online Education</p></div></div></div>`;$('#loginForm').onsubmit=async e=>{e.preventDefault();let f=new FormData(e.target);try{const d=await api('login',{method:'POST',body:{login:f.get('login'),password:f.get('password')}});state.user=d.user;renderShell();await loadPage('dashboard')}catch(x){$('#loginErr').textContent=x.message}}}
function renderOnboarding(){document.body.innerHTML=`<div class="login"><div class="loginBrand"><img src="/assets/logo.png"></div><div class="loginBox"><div class="panel"><h1>Complete your account</h1><p>Your Google account has been created. Set your system username and password.</p><form id="onForm"><div class="field"><label>Full name</label><input name="full_name" value="${esc(state.user.full_name)}" required></div><div class="field"><label>Username</label><input name="username" required minlength="3"></div><div class="field"><label>Password</label><input name="password" type="password" minlength="10" required></div><button class="btn primary block">Create profile</button><div id="onErr" class="error"></div></form></div></div></div>`;$('#onForm').onsubmit=async e=>{e.preventDefault();let f=new FormData(e.target);try{await api('onboarding',{method:'POST',body:Object.fromEntries(f)});state.user=(await api('me')).user;renderShell();await loadPage('dashboard')}catch(x){$('#onErr').textContent=x.message}}}
const nav=[['dashboard','▦','Dashboard'],['terms','◷','Terms & Calendar'],['courses','▤','Courses'],['levels','↥','Levels'],['trainers','♙','Trainers'],['students','♙','Students'],['distribution','✦','Smart Distribution'],['conflicts','⚠','Conflicts'],['schedule','☷','Published Schedule'],['ocr','▣','OCR / Import'],['reports','▥','Reports'],['archives','▤','Archives'],['myarchive','◫','My Archive'],['settings','⚙','Settings']];
function renderShell(){document.body.innerHTML=`<div class="shell"><aside class="sidebar"><div class="brand"><img src="/assets/logo.png"><strong>The First British Center<br>Online Education</strong></div><div class="nav">${nav.map(n=>`<button data-page="${n[0]}"><b>${n[1]}</b> <span>${n[2]}</span></button>`).join('')}<button id="logout"><b>↪</b> <span>Logout</span></button></div></aside><main class="main"><header class="top"><h2 id="pageTitle">Dashboard</h2><div class="userpill"><div><b>${esc(state.user.full_name)}</b><div style="font-size:11px;color:#7a8295">${esc(state.user.role)}</div></div><div class="avatar">${esc((state.user.full_name||'U')[0].toUpperCase())}</div></div></header><section id="content" class="content"></section></main></div><div id="modal" class="modal"><div class="modalBox"><div class="modalHead"><h3 id="modalTitle"></h3><button class="close" onclick="closeModal()">×</button></div><div id="modalBody"></div></div></div>`;$$('[data-page]').forEach(b=>b.onclick=()=>loadPage(b.dataset.page));$('#logout').onclick=async()=>{await api('logout',{method:'POST'});location.reload()}}
function setActive(p,title){state.page=p;$('#pageTitle').textContent=title;$$('[data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page===p))}
async function loadPage(p){try{const titles=Object.fromEntries(nav.map(n=>[n[0],n[2]]));setActive(p,titles[p]||'Dashboard');if(p==='dashboard')return pageDashboard();if(p==='terms')return pageTerms();if(p==='courses')return pageCourses();if(p==='levels')return pageLevels();if(p==='trainers')return pageTrainers();if(p==='students')return pageStudents();if(p==='distribution')return pageDistribution();if(p==='conflicts')return pageConflicts();if(p==='schedule')return pageSchedule();if(p==='ocr')return pageOCR();if(p==='reports')return pageReports();if(p==='archives')return pageArchives();if(p==='myarchive')return pageMyArchive();if(p==='settings')return pageSettings()}catch(e){$('#content').innerHTML=`<div class="card"><b>Could not load page</b><p class="error">${esc(e.message)}</p></div>`}}
function stat(k,n,l){return `<div class="card stat"><div><div class="label">${l}</div><div class="n">${n??0}</div></div><div style="font-size:24px;color:var(--gold)">◆</div></div>`}
async function pageDashboard(){const d=await api('dashboard');let c=d.counts;$('#content').innerHTML=`<div class="grid g4">${stat('courses',c.courses,'Courses')}${stat('trainers',c.trainers,'Trainers')}${stat('students',c.students,'Students')}${stat('terms',c.terms,'Terms')}${stat('conflicts',c.conflicts,'Student conflicts')}</div><div style="height:16px"></div><div class="grid g2"><div class="card"><div class="sectionTitle"><h3>Current Term</h3><span class="badge navy">${esc(d.current_term?.status||'—')}</span></div><div class="goldline"></div>${d.current_term?`<h2>${esc(d.current_term.name)}</h2><p>${esc(d.current_term.start_date)} → ${esc(d.current_term.end_date)} · ${d.current_term.required_teaching_days} teaching days</p>`:'<p>No term created yet.</p>'}</div><div class="card"><div class="sectionTitle"><h3>System Snapshot</h3></div><p><b>${c.schedule_entries}</b> schedule entries</p><p><b>${c.suspended_courses}</b> suspended courses</p><p>Audit trail is active for all critical operations.</p></div></div><div style="height:16px"></div><div class="card"><div class="sectionTitle"><h3>Recent Activity</h3></div><div class="tablewrap"><table class="table"><thead><tr><th>Time</th><th>User</th><th>Action</th><th>Entity</th></tr></thead><tbody>${d.recent.map(x=>`<tr><td>${esc(x.created_at)}</td><td>${esc(x.full_name||'System')}</td><td><span class="badge navy">${esc(x.action)}</span></td><td>${esc(x.entity)}</td></tr>`).join('')}</tbody></table></div></div>`}
function openModal(title,html){$('#modalTitle').textContent=title;$('#modalBody').innerHTML=html;$('#modal').classList.add('open')}function closeModal(){$('#modal').classList.remove('open')}
async function pageTerms(){const [d,h]=await Promise.all([api('terms'),api('holidays')]);state.cache.terms=d.data;$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">Terms & Academic Calendar</h2><p style="margin:5px 0;color:var(--muted)">20 teaching days, Friday excluded, and Admin-managed holidays.</p></div><div class="actions"><button class="btn primary" onclick="newTerm()">+ New Term</button><button class="btn gold" onclick="newHoliday()">+ Holiday</button></div></div><div class="card"><div class="tablewrap"><table class="table"><thead><tr><th>Term</th><th>Start</th><th>End</th><th>Teaching Days</th><th>Courses</th><th>Status</th></tr></thead><tbody>${d.data.map(t=>`<tr><td><b>${esc(t.name)}</b><br><small>Term ${t.term_number}</small></td><td>${t.start_date}</td><td>${t.end_date}</td><td>${t.required_teaching_days}</td><td>${t.course_count}</td><td><span class="badge ${t.status==='active'?'good':'navy'}">${t.status}</span></td></tr>`).join('')}</tbody></table></div></div><div style="height:16px"></div><div class="card"><div class="sectionTitle"><h3>Official / Admin Holidays</h3></div><div class="tablewrap"><table class="table"><thead><tr><th>Date</th><th>Name</th><th>Type</th><th>Status</th></tr></thead><tbody>${h.data.map(x=>`<tr><td>${x.holiday_date}</td><td>${esc(x.name)}</td><td>${esc(x.holiday_type)}</td><td>${x.active?'Active':'Inactive'}</td></tr>`).join('')}</tbody></table></div></div>`}
function newTerm(){openModal('Create Term',`<form id="termForm"><div class="formgrid"><div class="field"><label>Term number</label><input name="term_number" type="number" min="1"></div><div class="field"><label>Academic year</label><input name="academic_year" value="${new Date().getFullYear()}"></div><div class="field full"><label>Term name</label><input name="name" placeholder="Term 07" required></div><div class="field"><label>Start date</label><input name="start_date" type="date" required></div><div class="field"><label>Teaching days</label><input name="required_teaching_days" type="number" value="20" min="1"></div><div class="field"><label><input name="exclude_friday" type="checkbox" checked> Exclude Friday</label></div><div class="field full"><div id="termCalc" class="notice">End date will be calculated automatically from active holidays.</div></div></div><button class="btn primary">Create term</button><div id="formMsg"></div></form>`);$('#termForm').onsubmit=async e=>{e.preventDefault();let f=new FormData(e.target);try{const r=await api('create_term',{method:'POST',body:{...Object.fromEntries(f),exclude_friday:f.has('exclude_friday')?1:0}});toast('Term created');closeModal();pageTerms()}catch(x){$('#formMsg').innerHTML=`<div class="error">${esc(x.message)}</div>`}};$('#termForm [name=start_date]').onchange=async e=>{if(!e.target.value)return;let days=$('#termForm [name=required_teaching_days]').value;try{let r=await api('calculate_term',{method:'POST',body:{start_date:e.target.value,required_teaching_days:days,exclude_friday:1}});$('#termCalc').textContent=`Calculated end date: ${r.end_date} · ${r.days.length} teaching dates`;}catch(x){}}}
function newHoliday(){openModal('Add Holiday',`<form id="holidayForm"><div class="formgrid"><div class="field"><label>Date</label><input name="holiday_date" type="date" required></div><div class="field"><label>Type</label><select name="holiday_type"><option>official</option><option>center</option><option>emergency</option></select></div><div class="field full"><label>Name</label><input name="name" required></div><div class="field full"><label>Notes</label><textarea name="notes"></textarea></div></div><button class="btn gold">Save holiday</button></form>`);$('#holidayForm').onsubmit=async e=>{e.preventDefault();try{await api('create_holiday',{method:'POST',body:Object.fromEntries(new FormData(e.target))});toast('Holiday saved');closeModal();pageTerms()}catch(x){toast(x.message,true)}}}
async function pageCourses(){const d=await api('courses');state.cache.courses=d.data;$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">Courses</h2><p style="margin:5px 0;color:var(--muted)">Levels, capacity, session rules and active/suspended status.</p></div><button class="btn primary" onclick="newCourse()">+ Add Course</button></div><div class="card"><div class="tablewrap"><table class="table"><thead><tr><th>Course</th><th>Level</th><th>Time</th><th>Sessions/week</th><th>Students</th><th>Status</th></tr></thead><tbody>${d.data.map(x=>`<tr><td><b>${esc(x.name)}</b><br><small>${esc(x.code||'')}</small></td><td>${esc(x.level_name)}</td><td>${x.default_start||'—'}–${x.default_end||'—'}</td><td>${x.sessions_per_week}</td><td>${x.student_count}/${x.max_students}</td><td><span class="badge ${x.status==='active'?'good':x.status==='suspended'?'bad':'navy'}">${x.status}</span></td></tr>`).join('')}</tbody></table></div></div>`}
function newCourse(){openModal('Add Course',`<form id="courseForm"><div class="formgrid"><div class="field full"><label>Course name</label><input name="name" required></div><div class="field"><label>Code</label><input name="code"></div><div class="field"><label>Category</label><input name="category" value="English"></div><div class="field"><label>Level name</label><input name="level_name" value="A1" required></div><div class="field"><label>Level order</label><input name="level_order" type="number" value="1"></div><div class="field"><label>Maximum students</label><input name="max_students" type="number" value="25"></div><div class="field"><label>Default start</label><input name="default_start" type="time" value="18:00"></div><div class="field"><label>Default end</label><input name="default_end" type="time" value="20:00"></div><div class="field"><label>Weekday (1=Mon…7=Sun)</label><input name="default_weekday" type="number" min="1" max="7" value="0"></div><div class="field"><label>Sessions/week</label><input name="sessions_per_week" type="number" value="3"></div></div><button class="btn primary">Save course</button></form>`);$('#courseForm').onsubmit=async e=>{e.preventDefault();try{await api('create_course',{method:'POST',body:Object.fromEntries(new FormData(e.target))});toast('Course created');closeModal();pageCourses()}catch(x){toast(x.message,true)}}}
async function pageLevels(){const d=await api('levels');$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">Levels</h2><p style="margin:5px 0;color:var(--muted)">Admin controls the highest level available to the center. Trainer ranges use the numeric order.</p></div><button class="btn primary" onclick="newLevel()">+ Add Level</button></div><div class="card"><div class="tablewrap"><table class="table"><thead><tr><th>Level</th><th>Order</th><th>Status</th></tr></thead><tbody>${d.data.map(x=>`<tr><td><b>${esc(x.name)}</b></td><td>${x.level_order}</td><td>${x.active?'Active':'Inactive'}</td></tr>`).join('')}</tbody></table></div></div>`}
function newLevel(){openModal('Add Level',`<form id="levelForm"><div class="field"><label>Level name</label><input name="name" placeholder="C2" required></div><div class="field"><label>Level order</label><input name="level_order" type="number" min="1" required></div><button class="btn primary">Save level</button></form>`);$('#levelForm').onsubmit=async e=>{e.preventDefault();try{await api('create_level',{method:'POST',body:Object.fromEntries(new FormData(e.target))});toast('Level added');closeModal();pageLevels()}catch(x){toast(x.message,true)}}}
async function pageTrainers(){const d=await api('trainers');state.cache.trainers=d.data;$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">Trainers</h2><p style="margin:5px 0;color:var(--muted)">Eligibility is based on the configured level range and availability.</p></div><button class="btn primary" onclick="newTrainer()">+ Add Trainer</button></div><div class="card"><div class="tablewrap"><table class="table"><thead><tr><th>Trainer</th><th>Contact</th><th>Teaching range</th><th>Assigned entries</th><th>Status</th></tr></thead><tbody>${d.data.map(x=>`<tr><td><b>${esc(x.full_name)}</b></td><td>${esc(x.country_code||'')} ${esc(x.phone||'')}</td><td>${x.min_level_order} → ${x.max_level_order}</td><td>${x.assigned_count}</td><td><span class="badge ${x.status==='active'?'good':'bad'}">${x.status}</span></td></tr>`).join('')}</tbody></table></div></div>`}
function newTrainer(){openModal('Add Trainer',`<form id="trainerForm"><div class="formgrid"><div class="field full"><label>Full name</label><input name="full_name" required></div><div class="field"><label>Email</label><input name="email" type="email"></div><div class="field"><label>Country</label><input name="country"></div><div class="field"><label>Country code</label><input name="country_code" placeholder="+967"></div><div class="field"><label>Phone</label><input name="phone"></div><div class="field"><label>WhatsApp</label><input name="whatsapp"></div><div class="field"><label>Minimum level order</label><input name="min_level_order" type="number" value="1"></div><div class="field"><label>Maximum level order</label><input name="max_level_order" type="number" value="6"></div><div class="field full"><label>Notes</label><textarea name="notes"></textarea></div></div><div class="notice"><b>Weekly availability</b><br>Enter a start/end window for the days the trainer can teach. The scheduling engine will not assign outside these windows.</div><div class="tablewrap" style="margin-top:10px"><table class="table" style="min-width:0"><thead><tr><th>Day</th><th>Start</th><th>End</th></tr></thead><tbody>${['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'].map((day,i)=>`<tr><td>${day}</td><td><input data-av-day="${i+1}" data-av-start type="time" value="${i<4||i===6?'16:00':''}"></td><td><input data-av-day="${i+1}" data-av-end type="time" value="${i<4||i===6?'21:00':''}"></td></tr>`).join('')}</tbody></table></div><button class="btn primary" style="margin-top:12px">Save trainer</button></form>`);$('#trainerForm').onsubmit=async e=>{e.preventDefault();let base=Object.fromEntries(new FormData(e.target));base.availability=$$('[data-av-day]',e.target).reduce((arr,el)=>arr,[ ]);base.availability=[];for(let i=1;i<=7;i++){let st=$(`[data-av-day="${i}"][data-av-start]`,e.target)?.value,en=$(`[data-av-day="${i}"][data-av-end]`,e.target)?.value;if(st&&en)base.availability.push({weekday:i,start_time:st,end_time:en})}try{await api('create_trainer',{method:'POST',body:base});toast('Trainer created');closeModal();pageTrainers()}catch(x){toast(x.message,true)}}}
async function pageStudents(){const d=await api('students');state.cache.students=d.data;$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">Students</h2><p style="margin:5px 0;color:var(--muted)">Age is calculated from date of birth; country codes are preserved for contact reporting.</p></div><div class="actions"><button class="btn light" onclick="enrollStudent()">+ Enroll</button><button class="btn primary" onclick="newStudent()">+ Add Student</button></div></div><div class="card"><div class="tablewrap"><table class="table"><thead><tr><th>Student</th><th>Age</th><th>Country</th><th>Phone</th><th>Level</th><th>Status</th></tr></thead><tbody>${d.data.map(x=>`<tr><td><b>${esc(x.full_name)}</b><br><small>${esc(x.student_code||'')}</small></td><td>${x.age??'—'}</td><td>${esc(x.country||'')} ${esc(x.country_code||'')}</td><td>${esc(x.phone||'')}</td><td>${x.current_level_order}</td><td>${x.status}</td></tr>`).join('')}</tbody></table></div></div>`}
function newStudent(){openModal('Add Student',`<form id="studentForm"><div class="formgrid"><div class="field full"><label>Full name</label><input name="full_name" required></div><div class="field"><label>Student code</label><input name="student_code"></div><div class="field"><label>Date of birth</label><input name="date_of_birth" type="date"></div><div class="field"><label>Country</label><input name="country"></div><div class="field"><label>Country code</label><input name="country_code" placeholder="+967"></div><div class="field"><label>Phone</label><input name="phone"></div><div class="field"><label>WhatsApp</label><input name="whatsapp"></div><div class="field"><label>Email</label><input name="email" type="email"></div><div class="field"><label>Current level order</label><input name="current_level_order" type="number" value="1"></div><div class="field"><label>Parent/guardian</label><input name="parent_name"></div><div class="field"><label>Parent phone</label><input name="parent_phone"></div></div><button class="btn primary">Save student</button></form>`);$('#studentForm').onsubmit=async e=>{e.preventDefault();try{await api('create_student',{method:'POST',body:Object.fromEntries(new FormData(e.target))});toast('Student created');closeModal();pageStudents()}catch(x){toast(x.message,true)}}}
async function enrollStudent(){const [t,c,st]=await Promise.all([api('terms'),api('courses'),api('students')]);openModal('Enroll Student',`<form id="enrollForm"><div class="field"><label>Term</label><select name="term_id">${t.data.map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('')}</select></div><div class="field"><label>Student</label><select name="student_id">${st.data.filter(x=>x.status==='active').map(x=>`<option value="${x.id}">${esc(x.full_name)} · ${esc(x.country||'')}</option>`).join('')}</select></div><div class="field"><label>Course</label><select name="course_id">${c.data.filter(x=>x.status==='active').map(x=>`<option value="${x.id}">${esc(x.name)} · ${esc(x.level_name)}</option>`).join('')}</select></div><div class="notice">Capacity is checked before enrollment. The published schedule can then use these enrollments for student conflict reporting.</div><button class="btn primary">Enroll</button></form>`);$('#enrollForm').onsubmit=async e=>{e.preventDefault();try{await api('enroll_student',{method:'POST',body:Object.fromEntries(new FormData(e.target))});toast('Student enrolled');closeModal();pageStudents()}catch(x){toast(x.message,true)}}}
async function pageMyArchive(){const d=await api('my_archive');$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">My Archive</h2><p style="margin:5px 0;color:var(--muted)">Your own login/activity/change history.</p></div></div><div class="card"><div class="tablewrap"><table class="table"><thead><tr><th>Date</th><th>Action</th><th>Entity</th><th>Reason</th></tr></thead><tbody>${d.data.map(x=>`<tr><td>${x.created_at}</td><td><span class="badge navy">${esc(x.action)}</span></td><td>${esc(x.entity)} #${x.entity_id??''}</td><td>${esc(x.reason||'')}</td></tr>`).join('')}</tbody></table></div></div>`}
async function pageDistribution(){const [t,c]=await Promise.all([api('terms'),api('courses')]);state.cache.terms=t.data;state.cache.courses=c.data;$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">Smart Distribution Center</h2><p style="margin:5px 0;color:var(--muted)">Generate a proposed schedule, respecting trainer eligibility, availability, workload and repeat-gap rules.</p></div></div><div class="card"><div class="formgrid"><div class="field"><label>Term</label><select id="distTerm">${t.data.map(x=>`<option value="${x.id}">${esc(x.name)} · ${x.start_date}</option>`).join('')}</select></div><div class="field"><label>Mode</label><select id="distMode"><option value="simulate">Simulation — no database changes</option><option value="generate">Generate proposed schedule</option></select></div></div><div class="actions" style="display:flex;gap:8px"><button class="btn gold" onclick="generateDistribution()">✦ Generate</button><button class="btn light" onclick="assignAllCourses()">Assign all active courses to term</button></div></div><div style="height:16px"></div><div id="distResult"></div>`}
async function assignAllCourses(){let term=+$('#distTerm').value;let ids=state.cache.courses.filter(x=>x.status==='active').map(x=>+x.id);try{await api('assign_courses',{method:'POST',body:{term_id:term,course_ids:ids}});toast('Courses assigned to term')}catch(x){toast(x.message,true)}}
async function generateDistribution(){let term=+$('#distTerm').value;let simulate=$('#distMode').value==='simulate';try{const d=await api('generate_schedule',{method:'POST',body:{term_id:term,simulate}});$('#distResult').innerHTML=`<div class="grid g3">${stat('',d.created,'Generated sessions')}${stat('',d.unassigned.length,'Needs review')}${stat('',Object.values(d.trainer_load||{}).reduce((a,b)=>a+b,0),'Trainer assignments')}</div><div style="height:16px"></div><div class="card"><div class="sectionTitle"><h3>${simulate?'Simulation':'Proposed Schedule'}</h3></div><div class="tablewrap"><table class="table"><thead><tr><th>Date</th><th>Time</th><th>Course</th><th>Trainer</th></tr></thead><tbody>${d.proposal.map(x=>`<tr><td>${x.session_date}</td><td>${x.start_time}–${x.end_time}</td><td>${esc(x.course_name)}</td><td>${esc(x.trainer_name)}</td></tr>`).join('')}</tbody></table></div>${d.unassigned.length?`<div class="notice" style="margin-top:12px">${d.unassigned.map(x=>`⚠ ${esc(x.course)} — ${esc(x.date)} — ${esc(x.reason)}`).join('<br>')}</div>`:''}</div>`}catch(x){toast(x.message,true)}}
async function pageConflicts(){const t=await api('terms');let term=state.term||t.data[0]?.id;let d=term?await api('conflicts',{q:{term_id:term}}):{data:[]};$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">Conflict Center</h2><p style="margin:5px 0;color:var(--muted)">Student timetable overlaps detected from active enrollments and generated sessions.</p></div><select id="confTerm">${t.data.map(x=>`<option value="${x.id}" ${+x.id===+term?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="card"><div class="tablewrap"><table class="table"><thead><tr><th>Date</th><th>Student</th><th>Time</th><th>Course 1</th><th>Course 2</th></tr></thead><tbody>${d.data.length?d.data.map(x=>`<tr><td>${x.session_date}</td><td>${esc(x.student_name)}</td><td>${x.start_time}–${x.end_time}</td><td>${esc(x.course_one)}</td><td>${esc(x.course_two)}</td></tr>`).join(''):`<tr><td colspan="5">No student conflicts found.</td></tr>`}</tbody></table></div></div>`;$('#confTerm').onchange=e=>{state.term=e.target.value;pageConflicts()}}
async function pageSchedule(){const t=await api('terms');state.cache.terms=t.data;let term=state.term||t.data[0]?.id;state.term=term||'';const d=term?await api('schedule',{q:{term_id:term}}):{data:[]};$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">Published Schedule</h2><p style="margin:5px 0;color:var(--muted)">Approve and publish after reviewing the generated distribution.</p></div><div class="actions"><select id="scheduleTerm">${t.data.map(x=>`<option value="${x.id}" ${+x.id===+term?'selected':''}>${esc(x.name)}</option>`).join('')}</select><button class="btn primary" onclick="approveSchedule()">Approve</button><button class="btn gold" onclick="publishSchedule()">Publish</button><button class="btn light" onclick="printSchedule()">Print</button></div></div><div class="card"><div class="tablewrap"><table class="table"><thead><tr><th>Date</th><th>Time</th><th>Course</th><th>Trainer</th><th>Students</th><th>Status</th></tr></thead><tbody>${d.data.map(x=>`<tr><td>${x.session_date}</td><td>${x.start_time}–${x.end_time}</td><td><b>${esc(x.course_name)}</b><br><small>${esc(x.level_name)}</small></td><td>${esc(x.trainer_name||'Unassigned')}</td><td>${x.student_count}</td><td><span class="badge ${x.status==='published'?'good':x.status==='approved'?'navy':'warn'}">${x.status}</span></td></tr>`).join('')}</tbody></table></div></div>`;$('#scheduleTerm').onchange=e=>{state.term=e.target.value;pageSchedule()}}
async function approveSchedule(){try{await api('approve_schedule',{method:'POST',body:{term_id:+$('#scheduleTerm').value}});toast('Schedule approved');pageSchedule()}catch(x){toast(x.message,true)}}async function publishSchedule(){try{await api('publish_schedule',{method:'POST',body:{term_id:+$('#scheduleTerm').value}});toast('Schedule published');pageSchedule()}catch(x){toast(x.message,true)}}async function printSchedule(){const term=+$('#scheduleTerm').value;const sort=prompt('Sort by: time, course, trainer, status','time')||'time';const d=await api('print',{q:{term_id:term,sort}});const w=window.open('','_blank');w.document.write(`<html><head><title>The First British Center — Schedule</title><style>body{font-family:Arial;padding:25px;color:#18223b}img{width:120px}.head{text-align:center}.table{width:100%;border-collapse:collapse;margin-top:20px}.table th,.table td{border:1px solid #ddd;padding:8px;text-align:left}.table th{background:#eee}</style></head><body><div class="head"><img src="${location.origin}/assets/logo.png"><h2>The First British Center for Online Education</h2><h3>Course Distribution Schedule</h3></div><table class="table"><tr><th>Date</th><th>Time</th><th>Course</th><th>Trainer</th><th>Students</th><th>Status</th></tr>${d.data.map(x=>`<tr><td>${x.session_date}</td><td>${x.start_time}–${x.end_time}</td><td>${esc(x.course_name)}</td><td>${esc(x.trainer_name||'')}</td><td>${x.student_count}</td><td>${x.status}</td></tr>`).join('')}</table><script>window.print()</script></body></html>`);w.document.close()}
async function pageOCR(){$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">OCR / Import Center</h2><p style="margin:5px 0;color:var(--muted)">Upload JPG, PNG or PDF. If Tesseract is installed on the server, text extraction runs automatically.</p></div></div><div class="card"><form id="ocrForm"><div class="field"><label>Source file</label><input name="file" type="file" accept="image/jpeg,image/png,application/pdf" required></div><button class="btn gold">Run OCR</button></form><div id="ocrResult" style="margin-top:15px"></div></div>`;$('#ocrForm').onsubmit=async e=>{e.preventDefault();let f=new FormData(e.target);try{let d=await api('upload_ocr',{form:f});$('#ocrResult').innerHTML=`<div class="notice"><b>Status:</b> ${d.status}<br><br><pre style="white-space:pre-wrap">${esc(d.text||'')}</pre></div>`;toast('OCR import completed')}catch(x){toast(x.message,true)}}}
async function pageReports(){const t=await api('terms');$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">Reports & Print Center</h2><p style="margin:5px 0;color:var(--muted)">Build printable schedules by time, course, trainer or status.</p></div></div><div class="card"><div class="formgrid"><div class="field"><label>Term</label><select id="reportTerm">${t.data.map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('')}</select></div><div class="field"><label>Sort</label><select id="reportSort"><option value="time">Time</option><option value="course">Course name</option><option value="trainer">Trainer</option><option value="status">Status</option></select></div></div><button class="btn primary" onclick="openReport()">Generate report</button></div>`}
async function openReport(){let t=+$('#reportTerm').value,s=$('#reportSort').value;let d=await api('print',{q:{term_id:t,sort:s}});$('#content').insertAdjacentHTML('beforeend',`<div style="height:16px"></div><div class="card"><div class="printHeader"><img src="/assets/logo.png" style="width:120px"><h2>The First British Center for Online Education</h2></div><div class="sectionTitle"><h3>Report Preview</h3><button class="btn light noPrint" onclick="window.print()">Print</button></div><div class="tablewrap"><table class="table"><thead><tr><th>Date</th><th>Time</th><th>Course</th><th>Trainer</th><th>Students</th><th>Status</th></tr></thead><tbody>${d.data.map(x=>`<tr><td>${x.session_date}</td><td>${x.start_time}–${x.end_time}</td><td>${esc(x.course_name)}</td><td>${esc(x.trainer_name||'')}</td><td>${x.student_count}</td><td>${x.status}</td></tr>`).join('')}</tbody></table></div></div>`)}
async function pageArchives(){const d=await api('archives');$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">Master Archive & Audit</h2><p style="margin:5px 0;color:var(--muted)">Immutable-style operational history: who changed what, when and from which request.</p></div></div><div class="card"><div class="tablewrap"><table class="table"><thead><tr><th>Date</th><th>User</th><th>Action</th><th>Entity</th><th>Reason</th></tr></thead><tbody>${d.data.map(x=>`<tr><td>${x.created_at}</td><td>${esc(x.full_name||'System')}</td><td><span class="badge navy">${esc(x.action)}</span></td><td>${esc(x.entity)} #${x.entity_id??''}</td><td>${esc(x.reason||'')}</td></tr>`).join('')}</tbody></table></div></div>`}
async function pageSettings(){const d=await api('settings');let s=d.data;$('#content').innerHTML=`<div class="toolbar"><div><h2 style="margin:0">System Settings</h2><p style="margin:5px 0;color:var(--muted)">Scheduling policy is data-driven so Admin can change it without editing code.</p></div></div><div class="card"><form id="settingsForm"><div class="formgrid"><div class="field"><label>Teaching days / term</label><input name="term_teaching_days" value="${esc(s.term_teaching_days)}"></div><div class="field"><label>Minimum repeat gap (terms)</label><input name="minimum_repeat_gap_terms" value="${esc(s.minimum_repeat_gap_terms)}"></div><div class="field"><label>Exclude Friday</label><select name="exclude_friday"><option value="1" ${s.exclude_friday==='1'?'selected':''}>Yes</option><option value="0" ${s.exclude_friday==='0'?'selected':''}>No</option></select></div><div class="field"><label>Balance trainer workload</label><select name="balance_workload"><option value="1" ${s.balance_workload==='1'?'selected':''}>Yes</option><option value="0" ${s.balance_workload==='0'?'selected':''}>No</option></select></div><div class="field"><label>Default sessions/week</label><input name="default_sessions_per_week" value="${esc(s.default_sessions_per_week)}"></div><div class="field"><label>Default session minutes</label><input name="default_session_minutes" value="${esc(s.default_session_minutes)}"></div><div class="field full"><label>Center name</label><input name="center_name" value="${esc(s.center_name)}"></div><div class="field full"><label>Arabic center name</label><input name="center_name_ar" value="${esc(s.center_name_ar)}"></div></div><button class="btn primary">Save settings</button></form></div>`;$('#settingsForm').onsubmit=async e=>{e.preventDefault();try{await api('save_settings',{method:'POST',body:{settings:Object.fromEntries(new FormData(e.target))}});toast('Settings saved')}catch(x){toast(x.message,true)}}}
boot();
